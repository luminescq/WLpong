subprojects {
    afterEvaluate { project ->
        if (project.hasProperty("android")) {
            project.android {
                buildToolsVersion = "36.0.0"
                if (namespace == null) {
                    namespace project.group
                }
            }
        }
    }
}
